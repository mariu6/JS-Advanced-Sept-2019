function square( x = 5, delimiter = "* ") {     // Задавам стойности по подразбиране
    for (let i = 0; i < x; i++) {
        console.log(delimiter.repeat(x));
    }
}

square(3, "@ ");    // Ако не сложа аргументи, или сложа по-малко аргументи
                   // останалите аргументи ще се вземат по подразбиране. Но трябва да се спазва реда им.